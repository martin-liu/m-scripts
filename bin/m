#!/bin/bash
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="$DIR/../target/release/m"

if [[ ! -f "$BIN" ]]; then
    cargo build --release --manifest-path "$DIR/../Cargo.toml"
fi

exec "$BIN" "$@"
